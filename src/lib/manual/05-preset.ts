const section = {
  id: 'preset',
  title: '第五章 · 数据预设',
  content: `
<h4>5.1 可预设的内容</h4>
<ul>
  <li>客户（可绑定负责人）</li>
  <li>供应商</li>
  <li>来料数量</li>
  <li>工序</li>
  <li>临时处理方式</li>
  <li>负责人（常规 / 特殊分组）</li>
  <li>特殊分组（关联分类 + 负责人）</li>
  <li>共享预分类（生效范围勾选产品）</li>
  <li>预分组</li>
  <li>样品（样品 Tab）</li>
</ul>
<h4>5.2 ★ 特殊分组负责人自动联动</h4>
<blockquote>📘 <b>案例：客户绑定负责人 + 特殊分组命中</b><br>
① 数据预设 → 客户 → 新增「客户A」，勾选负责人 @张三<br>
② 数据预设 → 特殊分组 → 新增「特殊组A」
<ul>
  <li>分类：分类A、分类B</li>
  <li>负责人：@李四</li>
  <li>勾选「仅样品触发」</li>
</ul>
③ 主面板选一个「客户=客户A、样品=true」的产品，且含「分类A」<br>
④ 多产品汇总 → 负责人自动带出：<b>@张三 @李四</b><br>
⑤ 手动 ✕ 移除 @李四 → 切换产品组合后不再加回（否决集合）
</blockquote>
<h4>5.3 批量添加</h4>
<p>数据预设各 Tab 的「＋ 批量添加」使用统一弹窗；每行一个，或用逗号/顿号/分号分隔，重复项自动跳过。</p>
<h4>5.4 共享预分类生效范围</h4>
<p>每项共享预分类可设置：<code>null</code>（全部产品）/ <code>[]</code>（不生效）/ <code>[ids]</code>（部分产品）。</p>
`,
};
export default section;