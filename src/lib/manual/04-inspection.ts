const section = {
  id: 'inspection',
  title: '第四章 · 抽检数量与不良率',
  content: `
<h4>4.1 什么是抽检数量？</h4>
<p>抽检数量是<b>整个产品共用</b>的不良率分母。</p>

<h4>4.2 不良率计算</h4>
<p>每个分组的不良率 = 组内数量之和 ÷ 抽检数量。</p>

<h4>4.3 自动计算</h4>
<p>输入来料数量 → 抽检数量自动填入（AQL 一般水平Ⅱ · AQL=1.0）。</p>

<h4>4.4 手动调整</h4>
<p>产品信息卡片中 <code>[−] [+]</code> 微调或直接输入 → 所有分组同步。</p>

<blockquote>📘 <b>案例 A：手动改抽检数量</b>
<ol>
  <li>产品1 来料 66 → AQL 自动填 <b>13</b></li>
  <li>用户改为 <b>20</b></li>
  <li>分组"分组A"有 5 个不良 → 不良率 = 5/20 = <b>25%</b></li>
</ol>
</blockquote>

<blockquote>📘 <b>案例 B：改来料触发重置</b>
<ol>
  <li>产品1 来料 66，抽检数当前是 20（手改过）</li>
  <li>用户改来料为 <b>500</b></li>
  <li>抽检数量<b>自动变回 AQL(500) = 50</b></li>
</ol>
<p>⚠️ 每次改来料，都会丢弃之前的手改值。</p>
</blockquote>

<h4>4.5 分组显示</h4>
<p>分组卡片底部显示：<code>分类数 · 数量总和 · 不良率</code>。不良率格式为 <code>分子/分母 = %</code>，例如 <code>5/20 = 25%</code>。</p>

<h4>4.6 为什么统一为产品级？</h4>
<p>实际业务中同一产品的抽检数量是统一的（AQL 根据来料数量确定）。分组各自设置会造成数据不一致。v3.7 起统一为产品级。</p>
`,
};
export default section;